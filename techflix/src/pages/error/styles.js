import styled from "styled-components";

export const NotFoundContainer = styled.div`
  position: relative;
  overflow: hidden;
  width: 100%;
  /* padding-top: 25vh; */
  text-align: center;
  color: var(--white);
  @media (max-width: 800px) {
    font-size: 0.6rem;
  }
`;
